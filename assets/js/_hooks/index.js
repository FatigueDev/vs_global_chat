/*
This file was generated by the Surface compiler.
*/

function ns(hooks, nameSpace) {
  const updatedHooks = {}
  Object.keys(hooks).map(function(key) {
    updatedHooks[`${nameSpace}#${key}`] = hooks[key]
  })
  return updatedHooks
}

import * as c1 from "./VsGlobalChatWeb.Components.SendMessageComponent.hooks"

let hooks = Object.assign(
  ns(c1, "VsGlobalChatWeb.Components.SendMessageComponent")
)

export default hooks
